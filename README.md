# Juego de Triqui en Python

Este es un sencillo juego de triqui (tic-tac-toe) desarrollado en Python utilizando la biblioteca Pygame para la interfaz gráfica.

---

## Descripción

## El juego permite a dos jugadores alternar turnos para colocar sus símbolos ("X" y "O") en un tablero de 3x3.

## Características

- Interfaz gráfica simple y clara con imágenes para el fondo, la "X" y el "O".

- Cambio automático de turno entre los dos jugadores.

- Control del clic del mouse para colocar las fichas.

---

## Requisitos

- Python 3.x
- Pygame

Puedes instalar Pygame usando pip:

```bash
pip install pygame
```

## 💡 Recomendación: Entornos Virtuales (VENV)

Se recomienda encarecidamente utilizar un **entorno virtual** para este proyecto.

### ¿Por qué es útil?
- **Aislamiento**: Mantiene las librerías del proyecto separadas de tu instalación principal de Python.
- **Control de Versiones**: Evita conflictos si diferentes proyectos necesitan versiones distintas de la misma librería.
- **Limpieza**: Si deseas eliminar el proyecto, simplemente borras la carpeta `venv` y no quedan residuos en tu sistema.

### ¿Cómo configurarlo?

1. **Crear el entorno virtual**:
   ```bash
   python -m venv venv
   ```

2. **Activar el entorno**:
   - **Windows**:
     ```bash
     .\venv\Scripts\activate
     ```
   - **macOS / Linux**:
     ```bash
     source venv/bin/activate
     ```

3. **Instalar dependencias dentro del entorno**:
   ```bash
   pip install pygame
   ```
